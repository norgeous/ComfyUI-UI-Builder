import { createContext } from 'react';

const ComfyBridgeContext = createContext({
  // comfyUrl: undefined,
  // isGenerating: false,
  // progress: 0,
  // output: undefined,
});

export default ComfyBridgeContext;
