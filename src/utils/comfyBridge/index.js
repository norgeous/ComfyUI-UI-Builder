// new package to do the following (without react)

// - websockets
//   - find the ws!
//   - current node name
//   - progress
// - get objectInfo
// - queue
//   - executePrompt
//   - executeInterrupt
// - get image url

// eslint-disable-next-line no-unused-vars
const comfyBridge = ({ onChangeWs = () => {} }) => {
  const a = 1;
  return { a };
};

export default comfyBridge;
