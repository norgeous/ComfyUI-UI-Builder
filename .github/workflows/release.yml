name: pages-build

on:
  push:
    branches:
      - develop
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - closed

concurrency: preview-${{ github.ref }}

jobs:
  ci:
    name: 🦾 CI
    uses: ./.github/workflows/ci.yml
    with:
      ref: ${{ github.head_ref || github.ref_name }}

  deploy-preview:
    name: 🚀 Deploy preview to "gh-pages"
    needs: [ci]
    if: ${{ always() && !cancelled() && github.ref_name != 'develop' }}
    runs-on: ubuntu-latest
    steps:
      - name: ⤵️ Checkout "${{ github.head_ref || github.ref_name }}" branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}

      - name: ♻️ Restore gh-pages
        id: gh_pages_cache
        uses: actions/cache@v4
        with:
          path: gh-pages-directory
          key: ${{ needs.ci.outputs.CACHE_KEY_GH_PAGES }}
          fail-on-cache-miss: true

      - name: ✍️ Commit build to "gh-pages" branch
        uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: gh-pages-directory

  deploy-develop:
    name: 🚀 Deploy develop to "gh-pages"
    needs: [ci]
    if: ${{ always() && !cancelled() && github.ref_name == 'develop' }}
    runs-on: ubuntu-latest
    steps:
      - name: ⤵️ Checkout "gh-pages" branch
        uses: actions/checkout@v4
        with:
          ref: gh-pages

      - name: ♻️ Restore gh-pages
        id: gh_pages_cache
        uses: actions/cache@v4
        with:
          path: gh-pages-directory
          key: ${{ needs.ci.outputs.CACHE_KEY_GH_PAGES }}
          fail-on-cache-miss: true

      - name: ✍️ Commit build to "gh-pages" branch
        run: |
          rm -rf develop
          git config user.name github-actions
          git config user.email github-actions@github.com
          mv gh-pages-directory develop
          git add develop | tee -a $GITHUB_STEP_SUMMARY
          git commit -m "🚀 Deploy develop" | tee -a $GITHUB_STEP_SUMMARY
          git push | tee -a $GITHUB_STEP_SUMMARY

  release:
    name: 🚀 Release
    needs: [ci]
    if: ${{ !cancelled() && github.ref_name == 'develop' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: ⤵️ Checkout "release" branch
        uses: actions/checkout@v4
        with:
          ref: release

      - name: ♻️ Restore dist
        uses: actions/cache@v4
        with:
          path: |
            dist
            README.md
            __init__.py
          key: ${{ needs.ci.outputs.CACHE_KEY_VITE_DIST }}
          fail-on-cache-miss: true

      - name: ✍️ Commit to "release" branch
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git status | tee -a $GITHUB_STEP_SUMMARY
          git commit -am "🚀 Release from develop" | tee -a $GITHUB_STEP_SUMMARY
          git status | tee -a $GITHUB_STEP_SUMMARY
          # git push | tee -a $GITHUB_STEP_SUMMARY
