// import useAppContext from '../hooks/useAppContext';

const StatusBar = () => {
  // const {
  //   formState,
  //   bodyData,
  // } = useAppContext();

  return (
    <div>
      <div>StatusBar</div>
      {/* <pre>{JSON.stringify({formState},null,2)}</pre> */}
      {/* <pre style={{ whiteSpace: 'pre-wrap' }}>{JSON.stringify({bodyData},null,2)}</pre> */}
    </div>
  );
};

export default StatusBar;
